<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
        package="com.example.toko">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:networkSecurityConfig="@xml/network_security_config"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity android:name=".AdminAddMenuCombo"></activity>
        <activity android:name=".AdminAddEditMenuCombo" />
        <activity android:name=".AdminEditMenuProduct" />
        <activity android:name=".CustomEditText" />
        <activity android:name=".AdminAddMenuProduct" />
        <activity android:name=".AdminAddEditMenuProduct" />
        <activity android:name=".adminMainActivity" />
        <activity android:name=".orderEntry" />
        <activity android:name=".topUpEntry" />
        <activity android:name=".splash_screen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginRegister" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>